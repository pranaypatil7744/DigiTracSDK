#jdk:
#  - openjdk17
#
#before install:
#    - echo "Building and publishing library module
#
#install:
#  - ./gradlew :digitracksdk:assembleRelease :digitracksdk:publishToMavenLocal

jdk:
  - openjdk17

install:
  - ./gradlew :librarytrial:assembleRelease

artifacts:
  - path: librarytrial/build/outputs/aar/librarytrial-release.aar
    type: aar
