#jdk:
#  - openjdk17
#
#before install:
#    - echo "Building and publishing library module
#
#install:
#  - ./gradlew :digitracksdk:assembleRelease :digitracksdk:publishToMavenLocal

jdk:
  - openjdk17

install:
  - ./gradlew :digitracksdk:assembleRelease

artifacts:
  - path: digitracksdk/build/outputs/aar/digitracksdk-release.aar
    type: aar
